import type { ImageSourcePropType } from "react-native";

const linguaMascot = require("../../assets/images/moscot-logo.png") as ImageSourcePropType;

export const images = {
  linguaMascot,
} as const;
